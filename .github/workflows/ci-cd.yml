name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # Проверка сборки
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout кода
      uses: actions/checkout@v4
    
    - name: Установка Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: mini-app/package-lock.json
    
    - name: Установка Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true
    
    - name: Установка зависимостей фронтенда
      run: |
        cd mini-app
        npm ci
    
    - name: Сборка фронтенда
      run: |
        cd mini-app
        npm run build
    
    - name: Проверка Rust кода
      run: |
        cd signaling-server
        cargo check
    
    - name: Сборка Rust проекта
      run: |
        cd signaling-server
        cargo build --release

  # Автоматическое развертывание на сервере (только для main ветки)
  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    
    steps:
    - name: Checkout кода
      uses: actions/checkout@v4
      
    - name: Развертывание на сервере
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          cd ${{ secrets.SERVER_PROJECT_PATH || '/root/TelegramVoice' }}
          git pull origin main
          chmod +x deploy.sh
          ./deploy.sh
          
    - name: Пост-развертывание мониторинг
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          cd ${{ secrets.SERVER_PROJECT_PATH || '/root/TelegramVoice' }}
          echo "📊 Мониторинг после развертывания..."
          chmod +x monitor.sh
          ./monitor.sh
          
    - name: Проверка доступности сервисов
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          echo "🌐 Проверка доступности сервисов из внешней сети..."
          
          # Проверка WebSocket сервера
          if curl -s --max-time 5 telnet://${{ secrets.SERVER_HOST }}:8080 > /dev/null; then
            echo "✅ WebSocket сервер доступен извне"
          else
            echo "❌ WebSocket сервер недоступен извне"
            exit 1
          fi
          
          # Проверка HTTP сервера
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 http://${{ secrets.SERVER_HOST }}:3000 || echo "000")
          if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "404" ]; then
            echo "✅ HTTP сервер доступен извне (код: $HTTP_CODE)"
          else
            echo "❌ HTTP сервер недоступен извне (код: $HTTP_CODE)"
            exit 1
          fi
          
          echo "🎉 Все сервисы успешно развернуты и доступны!"