name: Simple Deploy

on:
  workflow_dispatch:  # Ручной запуск
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || (github.ref == 'refs/heads/main' && github.event_name == 'push')
    
    steps:
    - name: Deploy to server via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          cd ${{ secrets.SERVER_PROJECT_PATH || '/root/TelegramVoice' }}
          
          # Остановка старых процессов
          pm2 stop all || true
          
          # Сброс git состояния и обновление
          git reset --hard HEAD
          git clean -fd
          git pull origin main
          
          # Остановка всех PM2 процессов перед сборкой
          pm2 delete all || true
          
          # Сборка фронтенда
          cd mini-app
          npm install
          npm run build
          
          # Сборка Rust backend
          cd ../signaling-server
          if command -v cargo &> /dev/null; then
            cargo build --release
            echo "✅ Rust сервер собран"
          else
            echo "❌ Cargo не найден, устанавливаем Rust"
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            source ~/.cargo/env
            cargo build --release
          fi
          
          # Настройка Nginx (если еще не настроен)
          cd ..
          if [ ! -f /etc/nginx/sites-enabled/telegramvoice ]; then
            echo "🔧 Настройка Nginx..."
            chmod +x setup-nginx.sh
            ./setup-nginx.sh
          else
            echo "✅ Nginx уже настроен"
          fi
          
          # Запуск сервисов через PM2
          echo "🚀 Запуск сервисов..."
          
          # Остановка старых процессов PM2
          pm2 delete all || true
          
          # Запуск WebSocket сервера через smart start
          chmod +x smart-ws-start.sh
          ./smart-ws-start.sh || (chmod +x ultimate-ws-fix.sh && ./ultimate-ws-fix.sh)
          
          # Запуск фронтенда через PM2
          cd mini-app
          pm2 start ecosystem.config.js --only frontend 2>/dev/null || pm2 start npx --name "frontend" -- serve -s out -l 3000
          cd ..
          
          # Сохранение конфигурации PM2
          pm2 save
          
          # Проверка статуса
          pm2 status
          
          # Проверка портов
          echo "🔍 Проверка портов:"
          ss -tlnp | grep -E ":8080|:3000" || echo "Порты не найдены (ss)"
          lsof -i:8080 -i:3000 2>/dev/null || echo "Порты не найдены (lsof)"
          
          echo "✅ Развертывание завершено"
          
          # Финальная проверка доступности
          echo "🌐 Проверка доступности сервисов..."
          
          # Ожидание запуска сервисов
          echo "⏳ Ожидание запуска сервисов (10 секунд)..."
          sleep 10
          
          # Проверка WebSocket напрямую
          echo "🔍 Проверка WebSocket на порту 8080..."
          if timeout 5 bash -c "cat < /dev/tcp/localhost/8080" &>/dev/null; then
            echo "✅ WebSocket сервер доступен на порту 8080"
          else
            echo "❌ WebSocket сервер НЕ доступен на порту 8080"
            # Запуск ultimate fix
            echo "🚨 Запуск финального исправления..."
            chmod +x ultimate-ws-fix.sh
            ./ultimate-ws-fix.sh
          fi
          
          # Проверка HTTP фронтенда
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 http://localhost:3000 || echo "000")
          if [[ "$HTTP_STATUS" == "200" || "$HTTP_STATUS" == "404" ]]; then
            echo "✅ Фронтенд доступен (статус $HTTP_STATUS)"
          else
            echo "❌ Фронтенд недоступен (статус $HTTP_STATUS)"
          fi
          
          echo "🎉 Деплой завершен!"
          
    - name: Debug before final check
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          cd /root/TelegramVoice
          chmod +x quick-debug.sh
          ./quick-debug.sh
          
    - name: Final health check
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          echo "🌐 Проверка доступности..."
          # WebSocket
          if timeout 5 bash -c "cat < /dev/tcp/localhost/8080" &>/dev/null; then
            echo "✅ WS OK"
          else
            echo "❌ WS недоступен"
            echo "Запускаем финальное исправление..."
            cd /root/TelegramVoice
            chmod +x ultimate-ws-fix.sh
            ./ultimate-ws-fix.sh
            # Проверяем еще раз
            if timeout 5 bash -c "cat < /dev/tcp/localhost/8080" &>/dev/null; then
              echo "✅ WS восстановлен"
            else
              echo "❌ WS все еще недоступен"
              echo "🔥 Запускаем форсированный режим..."
              chmod +x force-ws-start.sh
              ./force-ws-start.sh
              # Последняя проверка
              if timeout 5 bash -c "cat < /dev/tcp/localhost/8080" &>/dev/null; then
                echo "✅ WS восстановлен форсированным запуском!"
              else
                echo "❌ КРИТИЧЕСКАЯ ОШИБКА: WebSocket не запускается!" && exit 1
              fi
            fi
          fi
          # HTTP
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 http://localhost:3000 || echo "000")
          if [[ "$HTTP_CODE" == "200" || "$HTTP_CODE" == "404" ]]; then
            echo "✅ HTTP OK (код $HTTP_CODE)"
          else
            echo "❌ HTTP недоступен (код $HTTP_CODE)" && exit 1
          fi
          echo "🎉 Деплой пройден успешно!"