name: Simple Deploy

on:
  workflow_dispatch:  # Ручной запуск
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || (github.ref == 'refs/heads/main' && github.event_name == 'push')
    
    steps:
    - name: Deploy to server via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          cd ${{ secrets.SERVER_PROJECT_PATH || '/root/TelegramVoice' }}
          
          # Остановка старых процессов
          pm2 stop all || true
          
          # Сброс git состояния и обновление
          git reset --hard HEAD
          git clean -fd
          git pull origin main
          
          # Остановка всех PM2 процессов перед сборкой
          pm2 delete all || true
          
          # Сборка фронтенда
          cd mini-app
          npm install
          npm run build
          
          # Сборка Rust backend
          cd ../signaling-server
          if command -v cargo &> /dev/null; then
            cargo build --release
            echo "✅ Rust сервер собран"
          else
            echo "❌ Cargo не найден, устанавливаем Rust"
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            source ~/.cargo/env
            cargo build --release
          fi
          
          # Настройка Nginx (если еще не настроен)
          cd ..
          if [ ! -f /etc/nginx/sites-enabled/telegramvoice ]; then
            echo "🔧 Настройка Nginx..."
            chmod +x setup-nginx.sh
            ./setup-nginx.sh
          else
            echo "✅ Nginx уже настроен"
          fi
          
          # Запуск сервисов через PM2
          echo "🚀 Запуск сервисов..."
          
          # Запуск WebSocket сервера
          pm2 start signaling-server/target/release/signaling-server --name "signaling-server" --log-date-format="YYYY-MM-DD HH:mm:ss Z"
          
          # Запуск фронтенда (статичные файлы)
          cd mini-app
          pm2 start npx --name "frontend" -- serve -s out -l 3000
          cd ..
          
          # Сохранение конфигурации PM2
          pm2 save
          
          # Проверка статуса
          pm2 status
          
          # Проверка портов
          echo "🔍 Проверка портов:"
          netstat -tlnp | grep -E ":8080|:3000" || echo "Порты не найдены"
          
          echo "✅ Развертывание завершено"